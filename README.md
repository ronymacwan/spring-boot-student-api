# Student Management REST API 

A simple app demonstrating REST APIs for a student management system in Java Spring Boot. It handles standard CRUD operations by decoupling web controllers, service logic, and database repositories. 

Key learnings: 
* Data transfer safety using DTO patterns 
* Data structure transformation with ModelMapper 
* Incremental updates using Java maps
